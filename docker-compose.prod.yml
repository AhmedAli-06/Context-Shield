version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: contextshield-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: contextshield
      POSTGRES_USER: cs_admin
      POSTGRES_PASSWORD: ${DB_PASSWORD:-cs_prod_password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cs_admin -d contextshield"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: contextshield-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend-base
    container_name: contextshield-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://cs_admin:${DB_PASSWORD:-cs_prod_password}@postgres:5432/contextshield
      REDIS_URL: redis://redis:6379/0
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-change-this-in-production}
      JWT_ACCESS_TOKEN_EXPIRE_MINUTES: "480"
      CORS_ORIGINS: '["https://app.contextshield.io"]'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: frontend-build
    container_name: contextshield-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
